import ScreenPlaceholder from '../components/ScreenPlaceholder.jsx'
import kk from '../i18n/kk.js'

// 4-қадам: толық POS экраны (категория + өнім + себет + заказ жасау).
export default function Pos() {
  return <ScreenPlaceholder title={kk.roles.pos} note="POS экраны 4-қадамда жасалады." />
}
